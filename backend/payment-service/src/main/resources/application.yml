project:
  version: @project.version@

spring:
  application:
    name: payment-service
  main.banner-mode: "off"
  sql.init.mode: never

  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${POSTGRES_DB}?currentSchema=${DB_SCHEMA:payments},public
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    hikari:
      schema: ${DB_SCHEMA:payments}
      maximum-pool-size: 30

  jpa:
    hibernate:
      ddl-auto: validate
      default_schema: ${DB_SCHEMA:payments}
    properties:
      hibernate.default_schema: ${DB_SCHEMA:payments}
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  flyway:
    enabled: true
    schemas: ${DB_SCHEMA:payments}
    default-schema: ${DB_SCHEMA:payments}
    table: ${FLYWAY_TABLE:flyway_history_payment}
    validate-on-migrate: true
    baseline-on-migrate: true
    locations:
      - classpath:db/migration
      - classpath:db/r

# Mercado Pago Configuration
mercadopago:
  access-token: ${MERCADOPAGO_ACCESS_TOKEN:APP_USR-3149021535390307-110914-55fe675713252fa4a2d138b203497d1f-2977404984}
  # URL del webhook (puerto 8080) - ngrok apunta al gateway
  notification-url: ${MERCADOPAGO_NOTIFICATION_URL:https://windy-nydia-noncoincident.ngrok-free.dev/webhook}
  webhook-secret: ${MERCADOPAGO_WEBHOOK_SECRET:}
  # URLs de redirección (puerto 3000) - ngrok apunta al frontend
  success-url: ${MERCADOPAGO_SUCCESS_URL:https://windy-nydia-noncoincident.ngrok-free.dev/my-tickets}
  failure-url: ${MERCADOPAGO_FAILURE_URL:https://windy-nydia-noncoincident.ngrok-free.dev/dashboard}
  pending-url: ${MERCADOPAGO_PENDING_URL:https://windy-nydia-noncoincident.ngrok-free.dev/my-tickets}

# Service URLs for inter-service communication
services:
  order:
    base-url: ${ORDER_SERVICE_URL:http://order-service:8080}
  ticket:
    base-url: ${TICKET_SERVICE_URL:http://ticket-service:8080}

# Frontend base URL for redirects - usa la misma URL de ngrok del gateway
# El gateway manejará las redirecciones al frontend local
app:
  frontend-base-url: ${FRONTEND_BASE_URL:https://windy-nydia-noncoincident.ngrok-free.dev}

logging.level:
  root: INFO
  org.hibernate.SQL: WARN
  org.hibernate.type.descriptor.sql.BasicBinder: WARN

management:
  endpoints.web.exposure.include: "health,info"
  endpoint.health.show-details: never
